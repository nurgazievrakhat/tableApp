-- Словарь токенов поискового индекса. Хранилища не занимает — это view
-- поверх items_fts. Нужен, чтобы исправлять опечатки в запросе по словам,
-- которые реально встречаются в прайсах, а не по общему словарю языка.
CREATE VIRTUAL TABLE items_vocab USING fts5vocab(items_fts, 'row');
